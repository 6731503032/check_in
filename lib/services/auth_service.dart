// lib/services/auth_service.dart
// Google Sign-In has been removed.
// Identity is now collected manually via the check-in form (name + email).
// This file is kept as a placeholder — safe to delete if unused.

class AuthService {
  // No authentication logic needed.
  // Student identity is validated locally by checking the email domain:
  // must end with @lamduan.mfu.ac.th
}